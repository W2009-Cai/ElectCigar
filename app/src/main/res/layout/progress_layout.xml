<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="@android:color/transparent">

    <ImageView
        android:id="@+id/loading_img"
        android:layout_width="@dimen/img_progress_width"
        android:layout_height="@dimen/img_progress_width"
        android:layout_gravity="center"
        android:src="@drawable/loading_frame_anim" />
    <TextView
        android:id="@+id/loading_txt"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="15dp"
        android:textSize="15sp"
        android:textColor="@color/gray_dark"
        android:text="@string/loading_now"
        android:visibility="gone"
        />

    <!--<ProgressBar-->
        <!--android:id="@+id/progressBar"-->
        <!--android:indeterminateDrawable="@drawable/loading_frame_anim"-->
        <!--android:indeterminate="true"-->
        <!--android:layout_width="wrap_content"-->
        <!--android:layout_height="wrap_content"-->
        <!--android:layout_gravity="center" />-->
</LinearLayout>